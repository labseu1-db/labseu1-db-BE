{
  "dependencies": {
    "express": "^4.16.4",
    "faker": "^4.1.0",
    "firebase": "^5.10.1",
    "firebase-admin": "^7.3.0"
  },
  "scripts": {
    "addSampleData": "cd addSampleData && node index.js",
    "test": "jest --verbose --watchAll"
  },
  "devDependencies": {
    "jest": "^24.7.1"
  }
}